export interface Address {
    name: string;
    description: string;
    address: string;
    postcode: string;
    city: string;
    telephone: string;
    web: string;
    email: string;
    contact?: string; // user
}
